app_identifier(ENV["BUNDLE_ID"] || "com.yourcompany.yourapp")
apple_id(ENV["APPLE_ID_EMAIL"] || "your@email.com")
team_id(ENV["APPLE_TEAM_ID"] || "YOUR_TEAM_ID")

# App Store Connect API Key (optional, for CI/CD)
# app_store_connect_api_key_path("./fastlane/api_key.json")
